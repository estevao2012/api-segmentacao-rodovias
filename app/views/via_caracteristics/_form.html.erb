<%= form_for([@rodovia, @via_caracteristic], url: {action: "create"} , remote: true, method: :post) do |f| %>
  <% if @via_caracteristic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@via_caracteristic.errors.count, "error") %> prohibited this via_caracteristic from being saved:</h2>
      <ul>
      <% @via_caracteristic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
  <% end %>

    <div class="form-group">
      <!-- <label for="exampleInputEmail1">Nome</label> -->
      <%= f.text_field :name, placeholder: "Nome", class: "form-control" %>
    </div>
    <div class="form-group">
      <!-- <label for="exapleInputEmail1">Nome</label> -->
      <%= f.text_area :description, placeholder: "Descrição", class: "form-control" %>
    </div>
    <div class="form-group">
      <!-- <label for="exampleInputEmail1">Nome</label> -->
      <%= f.select :via_caracteristic_category, ViaCaracteristicCategory.all.collect{|e| [e.name,e.id ]}, {prompt: "Categoria"}, { class: "form-control"} %>
    </div>
    
    <div class="form-group">
      Coord X: <%= f.text_field :coord_x, class: "coord-x-form form-control", readonly: true %>
      Coord Y: <%= f.text_field :coord_y, class: "coord-y-form form-control", readonly: true %>   
      </div>   
  </div>

  <div class="actions">
    <%= f.button type: :submit, class: "btn " do %>
      Cadastrar
    <% end%>
  </div>
<% end %>
